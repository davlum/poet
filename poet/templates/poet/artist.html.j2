{% extends 'layout/search.html' %}
{% from 'macros/composition.html' import render_compositions %}
{% from 'macros/util.html' import render_name, render_place, render_none %}
{% from 'macros/artist.html' import render_about %}

{% block resultbody %}
    <div class="center-block skinny">
        <h2>Autor</h2>
        <hr>
        <div>
            <h4>Nom</h4>
            <h3 class="indented">{{ render_name(artist) }}</h3>
        </div>
        <br>
        <div>
            <h4>Nac</h4>
            <h3 class="indented">
                {{ render_none(artist.fecha_comienzo) }}
            </h3>
            <h3 class="indented">
                {{ render_place(artist) }}
            </h3>
            <br>
        </div>
        <div>
        {% if artist.fecha_finale is not none %}
            <h4>Muer</h4>
            <h3 class="indented">
               {{ artist.fecha_finale }}
            </h3>
            <h3 class="indented">
                {{ render_place(artist, muer=True) }}
            </h3>
            <br>
        {% endif %}
        </div>
        {{ render_about(artist) }}
        <hr>
        {{ render_compositions(compositions) }}
        {% if performances|length > 0 %}
            <hr>
            <div>
                <h3>Realizado</h3>
                {{ render_compositions(performances) }}
            </div>
        {% endif %}
    </div>
{% endblock %}
