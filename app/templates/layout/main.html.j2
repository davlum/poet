<!DOCTYPE html>
<html lang="es" data-predefined-style="true" data-css-presets="true">
    <head>
        <meta http-equiv="Content-Type" content="text/html; charset=utf-8">
        <title>PoéticaSonora | México 2016</title>
        <meta name="description" content="Voz, legibilidad, escucha">
        <meta http-equiv="X-UA-Compatible" content="IE=edge,chrome=1">
        <meta name="viewport" content="initial-scale=1.0, maximum-scale=1.0, user-scalable=no">

        <!-- Eliminate Caching for development -->
        <meta http-equiv="cache-control" content="max-age=0" />
        <meta http-equiv="cache-control" content="no-cache" />
        <meta http-equiv="expires" content="0" />
        <meta http-equiv="expires" content="Tue, 01 Jan 1980 1:00:00 GMT" />
        <meta http-equiv="pragma" content="no-cache" />
        <meta name="csrf-token" content="{{ csrf_token }}">

        <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/4.7.0/css/font-awesome.min.css">
        <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css">
        <link href="{{ static('css/global.css') }}" id="" rel="stylesheet" type="text/css" />
        <link href="{{ static('css/searchbar.css') }}" id="" rel="stylesheet" type="text/css" />
        {% block css %}{% endblock %}
    </head>
    <body data-bodyview="index" class="default">
		<customhtml></customhtml>
		<div class="loading" data-loading="page">
			<div class="loading_animation pulsing <%= extra_class %> "><div>
                          </div>
                        </div>
                </div>



    <!-- Modal -->
    <div class="modal fade" id="confirm-modal" role="dialog">
        <div class="vertical-alignment-helper">
            <div class="modal-dialog vertical-alignment-center">
                <!-- Modal content-->
                <div class="modal-content">
                    <div class="modal-header">
                        <button type="button" class="close" data-dismiss="modal">&times;</button>
                        <h4 class="modal-title">¿Estas seguro que quieres borrarlo?</h4>
                    </div>
                    <div class="modal-body text-center">
                        <a href="" id="delete-button" type="button"
                            class="btn btn-danger">Borrar</a>
                        <button type="button" class="btn btn-default"
                                data-dismiss="modal">Cerca</button>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <div class="modal fade" id="progress-modal" role="dialog">
        <div class="vertical-alignment-helper">
            <div class="modal-dialog vertical-alignment-center">
                <!-- Modal content-->
                <div class="modal-content">
                    <div class="modal-header text-center">
                        <h4 class="modal-title">Por favor espera</h4>
                    </div>
                    <div class="modal-body text-center">
                        <div class="progress">
                            <div id="progressBar" class="progress-bar" role="progressbar"
                            aria-valuenow="0" aria-valuemin="0" aria-valuemax="100" style="width: 0%;">
                                0%
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>

        {% include 'includes/_navbar.html.j2' %}
        <div id="backdrop" data-view="Backdrop" data-set="Backdrop">
        </div>
        <div class="container center-block" style="width: 80%;">

            <!-- messages -->
            <div class="row">
            <div class="col-md-12" id="flash-message-div"></div>
            </div>
            {% for message in get_messages(request) %}
                <li{% if message.tags %} class="{{ message.tags }}"{% endif %}>{{ message }}</li>
            {% endfor %}

            <!-- child template -->
            {% block body %}{% endblock %}

            <!-- errors -->
            {% if error %}
                <p class="error"><strong>Error:</strong> {{ error }}</p>
            {% endif %}


            <directlink link="http://poeticasonora.mx/PS-Inicio" id="dl_145850"></directlink>
            <div data-subpage-overlay class="subpage_overlay"></div>
            <div data-content-ruler class="content_width"></div>

        </div>

        <!-- scripts -->
        <script src ="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/js/bootstrap.min.js"></script>
        <script src="https://unpkg.com/wavesurfer.js"></script>
        <script src="{{ static('js/init_wavesurfer.js') }}"></script>
        {% block js %}{% endblock %}
    </body>
</html>
